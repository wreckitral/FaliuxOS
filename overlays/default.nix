{ inputs, ... }:
[
  # Example: pull a package from nixpkgs-unstable when stable is behind.
  # (uncomment and adjust once actually need it)
  #
  # (final: prev: {
  #   somePackage = inputs.nixpkgs-unstable.legacyPackages.${prev.system}.somePackage;
  # })
]
